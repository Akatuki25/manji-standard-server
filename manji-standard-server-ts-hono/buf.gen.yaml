version: v2
plugins:
  # カスタムプラグイン: proto → Entity / Repository / Mock / Postgres(Drizzle) / Usecase interface / REST Handler / handler-registry を生成
  - local: ["node", "./tools/mss-protoc-gen/index.mjs"]
    out: .
    opt:
      - target=ts
