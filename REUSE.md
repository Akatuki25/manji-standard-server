# REUSE — このリポジトリを基盤(scaffold)に新しいアプリを作る手順

このリポジトリは「動く標準サーバ + proto-first 生成器」のセットであり、新しいドメインのアプリは
**スタックディレクトリを丸ごとコピーして所有する**ことから始める。

## 原則

- **丸ごとコピーする。個別ファイルのつまみ食いをしない。** スタックは生成器(`tools/`)・共有カーネル
  (`app/infra/db.py`, `app/domain/entity/base.py` 等)・設定(`pyproject.toml`, CI)・手書きの雛形(service/usecase/tests)が
  一体で動くよう設計されている。一部だけ抜くと壊れ方が分かりにくい。
- **コピーした側(アプリ)が所有する。** 以後アプリ側で自由に書き換えてよい。ただし——
- **生成器・テンプレの不具合や不足を見つけたら、アプリ側で握りつぶさず必ずこのリポジトリに還流する**(branch → PR)。
  これが dogfooding の目的。アプリ側の生成器コピーは還流後に上書き同期する。

## 手順

### 0. 新リポジトリを作る

```
<app>/
├── proto/<domain>/v1/<domain>.proto   # 単一の真実
├── backend/                           # ← manji-standard-server-python の丸ごとコピー
├── web/                               # ← manji-standard-server-web の丸ごとコピー
├── scripts/check-contract.sh          # ← コピー
└── .gitignore                         # .venv/ __pycache__/ node_modules/ .next/ など
```

### 1. スタックを丸ごとコピー

```bash
cp -R manji-standard-server-python <app>/backend
cp -R manji-standard-server-web    <app>/web
cp scripts/check-contract.sh       <app>/scripts/
# コピー後に掃除(実行環境・キャッシュは持ち込まない):
rm -rf <app>/backend/.venv <app>/backend/uv.lock <app>/web/node_modules <app>/web/.next
find <app> -name __pycache__ -type d -exec rm -rf {} + ; find <app> -name .DS_Store -delete
```

`backend/pyproject.toml` の `name`/`description` をアプリ名に変える。依存管理は **uv**(`uv sync` が
`[dependency-groups]` の dev も入れる。pip の `-e .[dev]` は使えない)。

### 2. ドメイン proto を書く

- `<app>/proto/<domain>/v1/<domain>.proto` に Annotation DSL で entity + service を定義:
  - field: `@entity @pk @paging @unique @email @required @timestamp`(`int64 <name>_unix`)
  - frontend 表示制御: `@label <表示名>` `@list primary|secondary|hidden`
  - rpc: `@http METHOD /path`(path 変数は `{id}`)。`<Name>Service` の `<Name>` が entity 名(manji 規約)
  - 1ファイルに複数 entity + 複数 service を書いてよい(生成器は service ループ駆動)
- 同じファイルを `backend/proto/` と `web/proto/` にも置く(スタックローカルコピー)。
  一致は `scripts/check-contract.sh` で検査する。
- サンプルの `user/v1/user.proto` は3箇所とも削除。

### 3. 生成器を回す

```bash
# backend (protoc が必要 / buf 不要)
cd <app>/backend
uv sync
uv run tools/mss-protoc-gen/main.py <domain>/v1/<domain>.proto   # DDD 9層を生成
uv run tools/mss-migration-gen/main.py                            # Entity → up.sql

# web (buf/protoc 不要 = proto 直パース)
cd <app>/web
npm ci
npm run gen -- <domain>/v1/<domain>.proto
```

### 4. サンプル(user)由来物を置き換える

再生成では**消えない**ので手で削除・書き換えする:

| 種別 | 対象 | やること |
|---|---|---|
| 生成物 | `backend/app/**/{user,mock_user}*` / `web/src/gen/**/user*` / `web/src/app/users/` | 削除 |
| migration | `backend/migrations/*.up.sql` と `.snapshot.json` | 削除してから migration-gen を回す(新規アプリはゼロから) |
| 手書き雛形 | `backend/app/domain/service/user_service.py` / `app/usecase/user_usecase.py` / `app/main.py` の wire / `tests/` | user 版を雛形に新ドメイン版を書く |
| 手書き雛形 | `web/src/app/(home)` 等の user 参照 | 新ドメインに合わせる |

### 5. アプリ固有の手書き

- backend: Service/Usecase のロジック・tx 境界・業務固有 route(生成 handler と共存可)
- web: `componentMap`(widget 実体/テーマ)・業務固有画面
- **生成物ヘッダ(`Code generated by mss-protoc-gen. DO NOT EDIT.`)のあるファイルは手編集しない**

### 6. コンテナで動かす

各スタックは Dockerfile + docker-compose.yml を同梱している(全スタック共通の規約):

```bash
cd <app>/backend && docker compose up --build   # api(:8000) + db(postgres:16)。起動時に tools/apply-migrations.py が migrations/*.up.sql を冪等適用
cd <app>/web && docker compose up --build       # web(:3000)。NEXT_PUBLIC_API_BASE は build arg で注入
```

アプリ側では root に統合 compose を置き、backend/web/db をまとめて上げる構成を推奨。

### 7. CI を移植する

- codegen-drift(生成器を再実行 → 差分ゼロを強制)
- contract-check(root proto と各スタック proto の一致)
- unit / integration(postgres service container)

## 生成器の引数(このドキュメントの前提)

- Python: `uv run tools/mss-protoc-gen/main.py [proto相対パス]`(省略時 `user/v1/user.proto`)
- web: `npm run gen -- [proto相対パス]`(省略時 `proto/user/v1/user.proto`)
